import type { LedgerScope } from "../ledger/scope";

export async function recognizeRevenue(
  _scope: LedgerScope,
  _from: Date,
  _to: Date,
): Promise<void> {
  // Placeholder: revenue recognition will be implemented when
  // contract management and milestone tracking are added.
}
